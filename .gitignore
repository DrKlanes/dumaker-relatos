# Copias originales sin optimizar (no se publican; respaldo local)
_originals/

# Miniaturas crudas descargadas de YouTube (origen de los posters)
_posters_raw/

# Script local de optimización de imágenes (no forma parte del sitio)
_build_assets.py

# Verificación local (cotejo de textos) — no forma parte del sitio
_verify/

# CNAME: preparado pero NO activado todavía. Si se sube, GitHub fija el dominio
# personalizado y redirige la URL github.io (que no resolvería hasta tener DNS).
# Se activa (quitar esta línea + commit) cuando el DNS de cdmon esté puesto.
CNAME

# Archivo temporal de la descarga de fuentes
fonts-source.css

# Herramientas locales de previsualización
.claude/

# Basura del sistema
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
